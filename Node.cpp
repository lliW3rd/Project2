#include "Node.h"
// each node in the binary tree points to a hash table

void node::setRawEvent(string event)
{
	rawEvent = event;
}

void node::insertNode(string event)
{

	rawEvent = event;

}
